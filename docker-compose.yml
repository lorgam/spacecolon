version: '3'

services:
  webserver:
    image: httpd:2.4
    volumes:
      - ./:/usr/local/apache2/htdocs:ro
    ports:
      - 80:80
  node:
    image: node:15-alpine
    volumes:
       - ./:/node
    working_dir: /node

